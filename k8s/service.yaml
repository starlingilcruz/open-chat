apiVersion: v1
kind: Service
metadata:
  name: openchat-web
  namespace: openchat
  labels:
    app: openchat
    component: web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: openchat
    component: web
