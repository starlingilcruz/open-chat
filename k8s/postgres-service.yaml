apiVersion: v1
kind: Service
metadata:
  name: openchat-db
  namespace: openchat
  labels:
    app: openchat
    component: db
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: openchat
    component: db
