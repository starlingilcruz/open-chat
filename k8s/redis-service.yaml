apiVersion: v1
kind: Service
metadata:
  name: openchat-redis
  namespace: openchat
  labels:
    app: openchat
    component: redis
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: openchat
    component: redis
